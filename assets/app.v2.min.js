const API_KEY = '9c6520bc940a44bbb5e6cad8ceea0e16',
  WC_COMPETITION_CODE = 'WC',
  WC_COMPETITION_ID = 2e3,
  WORLD_CUPS = [
    {
      year: 2026,
      start: '2026-06-11',
      end: '2026-07-19',
      hosts: ['USA', 'MEX', 'CAN'],
      hostNames: 'USA · Mexique · Canada'
    },
    {
      year: 2030,
      start: '2030-06-01',
      end: '2030-07-15',
      hosts: ['ESP', 'POR', 'MAR'],
      hostNames: 'Espagne · Portugal · Maroc'
    },
    { year: 2034, start: '2034-06-01', end: '2034-07-15', hosts: ['SAU'], hostNames: 'Arabie Saoudite' },
    { year: 2038, start: '2038-06-01', end: '2038-07-15', hosts: ['TBD'], hostNames: 'A confirmer' },
    { year: 2042, start: '2042-06-01', end: '2042-07-15', hosts: ['TBD'], hostNames: 'A confirmer' }
  ],
  STADIUM_TIMEZONES = {
    'MetLife Stadium': 'America/New_York',
    'AT&T Stadium': 'America/Chicago',
    'Mercedes-Benz Stadium': 'America/New_York',
    'Arrowhead Stadium': 'America/Chicago',
    'NRG Stadium': 'America/Chicago',
    "Levi's Stadium": 'America/Los_Angeles',
    'SoFi Stadium': 'America/Los_Angeles',
    'Lincoln Financial Field': 'America/New_York',
    'Lumen Field': 'America/Los_Angeles',
    'Gillette Stadium': 'America/New_York',
    'Hard Rock Stadium': 'America/New_York',
    'Estadio Azteca': 'America/Mexico_City',
    'Estadio BBVA': 'America/Monterrey',
    'Estadio Akron': 'America/Mexico_City',
    'BMO Field': 'America/Toronto',
    'BC Place': 'America/Vancouver'
  },
  STADIUM_CITIES = {
    'MetLife Stadium': 'East Rutherford, NJ',
    'AT&T Stadium': 'Arlington, TX',
    'Mercedes-Benz Stadium': 'Atlanta, GA',
    'Arrowhead Stadium': 'Kansas City, MO',
    'NRG Stadium': 'Houston, TX',
    "Levi's Stadium": 'Santa Clara, CA',
    'SoFi Stadium': 'Inglewood, CA',
    'Lincoln Financial Field': 'Philadelphia, PA',
    'Lumen Field': 'Seattle, WA',
    'Gillette Stadium': 'Foxborough, MA',
    'Hard Rock Stadium': 'Miami Gardens, FL',
    'Estadio Azteca': 'Mexico City',
    'Estadio BBVA': 'Guadalupe, NL',
    'Estadio Akron': 'Zapopan',
    'BMO Field': 'Toronto',
    'BC Place': 'Vancouver'
  },
  STADIUM_COUNTRIES = {
    'MetLife Stadium': 'USA',
    'AT&T Stadium': 'USA',
    'Mercedes-Benz Stadium': 'USA',
    'Arrowhead Stadium': 'USA',
    'NRG Stadium': 'USA',
    "Levi's Stadium": 'USA',
    'SoFi Stadium': 'USA',
    'Lincoln Financial Field': 'USA',
    'Lumen Field': 'USA',
    'Gillette Stadium': 'USA',
    'Hard Rock Stadium': 'USA',
    'Estadio Azteca': 'Mexique',
    'Estadio BBVA': 'Mexique',
    'Estadio Akron': 'Mexique',
    'BMO Field': 'Canada',
    'BC Place': 'Canada'
  },
  STADIUM_ID_TO_NAME = {
    1: 'Estadio Azteca',
    2: 'Estadio Akron',
    3: 'Estadio BBVA',
    4: 'AT&T Stadium',
    5: 'NRG Stadium',
    6: 'Arrowhead Stadium',
    7: 'Mercedes-Benz Stadium',
    8: 'Hard Rock Stadium',
    9: 'Gillette Stadium',
    10: 'Lincoln Financial Field',
    11: 'MetLife Stadium',
    12: 'BMO Field',
    13: 'BC Place',
    14: 'Lumen Field',
    15: "Levi's Stadium",
    16: 'SoFi Stadium'
  },
  WCUP2026_GROUND_TO_STADIUM = {
    Atlanta: 'Mercedes-Benz Stadium',
    'Boston (Foxborough)': 'Gillette Stadium',
    'Dallas (Arlington)': 'AT&T Stadium',
    'Guadalajara (Zapopan)': 'Estadio Akron',
    Houston: 'NRG Stadium',
    'Kansas City': 'Arrowhead Stadium',
    'Los Angeles (Inglewood)': 'SoFi Stadium',
    'Mexico City': 'Estadio Azteca',
    'Miami (Miami Gardens)': 'Hard Rock Stadium',
    'Monterrey (Guadalupe)': 'Estadio BBVA',
    'New York/New Jersey (East Rutherford)': 'MetLife Stadium',
    Philadelphia: 'Lincoln Financial Field',
    'San Francisco Bay Area (Santa Clara)': "Levi's Stadium",
    Seattle: 'Lumen Field',
    Toronto: 'BMO Field',
    Vancouver: 'BC Place'
  };
let currentWC = null,
  refreshInterval = null,
  goalsChartInstance = null,
  palmaresChartInstance = null,
  computeCache = {},
  wc26FetchPromise = null,
  lastWc26FetchSource = null,
  wc26ApiFailures = 0,
  wc26FirstFetchDone = !1,
  isPageVisible = !document.hidden,
  lastMatchesSource = null;
setInterval(() => {
  wc26ApiFailures > 0 && (console.log('[worldcup26] reset failure counter after cooldown'), (wc26ApiFailures = 0));
}, 3e5);
const STATIC_FALLBACK_TEAMS = {
    2026: [
      { name: 'Mexique', code: 'mx', confederation: 'CONCACAF', group: 'Groupe A' },
      { name: 'Afrique du Sud', code: 'za', confederation: 'CAF', group: 'Groupe A' },
      { name: 'Corée du Sud', code: 'kr', confederation: 'AFC', group: 'Groupe A' },
      { name: 'Tchéquie', code: 'cz', confederation: 'UEFA', group: 'Groupe A' },
      { name: 'Canada', code: 'ca', confederation: 'CONCACAF', group: 'Groupe B' },
      { name: 'Bosnie-Herzégovine', code: 'ba', confederation: 'UEFA', group: 'Groupe B' },
      { name: 'Qatar', code: 'qa', confederation: 'AFC', group: 'Groupe B' },
      { name: 'Suisse', code: 'ch', confederation: 'UEFA', group: 'Groupe B' },
      { name: 'Brésil', code: 'br', confederation: 'CONMEBOL', group: 'Groupe C' },
      { name: 'Maroc', code: 'ma', confederation: 'CAF', group: 'Groupe C' },
      { name: 'Haïti', code: 'ht', confederation: 'CONCACAF', group: 'Groupe C' },
      { name: 'Écosse', code: 'gb-sct', confederation: 'UEFA', group: 'Groupe C' },
      { name: 'États-Unis', code: 'us', confederation: 'CONCACAF', group: 'Groupe D' },
      { name: 'Paraguay', code: 'py', confederation: 'CONMEBOL', group: 'Groupe D' },
      { name: 'Australie', code: 'au', confederation: 'AFC', group: 'Groupe D' },
      { name: 'Turquie', code: 'tr', confederation: 'UEFA', group: 'Groupe D' },
      { name: 'Allemagne', code: 'de', confederation: 'UEFA', group: 'Groupe E' },
      { name: 'Curaçao', code: 'cw', confederation: 'CONCACAF', group: 'Groupe E' },
      { name: "Côte d'Ivoire", code: 'ci', confederation: 'CAF', group: 'Groupe E' },
      { name: 'Équateur', code: 'ec', confederation: 'CONMEBOL', group: 'Groupe E' },
      { name: 'Pays-Bas', code: 'nl', confederation: 'UEFA', group: 'Groupe F' },
      { name: 'Japon', code: 'jp', confederation: 'AFC', group: 'Groupe F' },
      { name: 'Suède', code: 'se', confederation: 'UEFA', group: 'Groupe F' },
      { name: 'Tunisie', code: 'tn', confederation: 'CAF', group: 'Groupe F' },
      { name: 'Belgique', code: 'be', confederation: 'UEFA', group: 'Groupe G' },
      { name: 'Égypte', code: 'eg', confederation: 'CAF', group: 'Groupe G' },
      { name: 'Iran', code: 'ir', confederation: 'AFC', group: 'Groupe G' },
      { name: 'Nouvelle-Zélande', code: 'nz', confederation: 'OFC', group: 'Groupe G' },
      { name: 'Espagne', code: 'es', confederation: 'UEFA', group: 'Groupe H' },
      { name: 'Cap-Vert', code: 'cv', confederation: 'CAF', group: 'Groupe H' },
      { name: 'Arabie Saoudite', code: 'sa', confederation: 'AFC', group: 'Groupe H' },
      { name: 'Uruguay', code: 'uy', confederation: 'CONMEBOL', group: 'Groupe H' },
      { name: 'France', code: 'fr', confederation: 'UEFA', group: 'Groupe I' },
      { name: 'Sénégal', code: 'sn', confederation: 'CAF', group: 'Groupe I' },
      { name: 'Irak', code: 'iq', confederation: 'AFC', group: 'Groupe I' },
      { name: 'Norvège', code: 'no', confederation: 'UEFA', group: 'Groupe I' },
      { name: 'Argentine', code: 'ar', confederation: 'CONMEBOL', group: 'Groupe J' },
      { name: 'Algérie', code: 'dz', confederation: 'CAF', group: 'Groupe J' },
      { name: 'Autriche', code: 'at', confederation: 'UEFA', group: 'Groupe J' },
      { name: 'Jordanie', code: 'jo', confederation: 'AFC', group: 'Groupe J' },
      { name: 'Portugal', code: 'pt', confederation: 'UEFA', group: 'Groupe K' },
      { name: 'RD Congo', code: 'cd', confederation: 'CAF', group: 'Groupe K' },
      { name: 'Ouzbékistan', code: 'uz', confederation: 'AFC', group: 'Groupe K' },
      { name: 'Colombie', code: 'co', confederation: 'CONMEBOL', group: 'Groupe K' },
      { name: 'Angleterre', code: 'gb-eng', confederation: 'UEFA', group: 'Groupe L' },
      { name: 'Croatie', code: 'hr', confederation: 'UEFA', group: 'Groupe L' },
      { name: 'Ghana', code: 'gh', confederation: 'CAF', group: 'Groupe L' },
      { name: 'Panama', code: 'pa', confederation: 'CONCACAF', group: 'Groupe L' }
    ]
  },
  TEAM_TRANSLATIONS = {
    Mexico: { fr: 'Mexique', code: 'mx' },
    'South Africa': { fr: 'Afrique du Sud', code: 'za' },
    'South Korea': { fr: 'Corée du Sud', code: 'kr' },
    'Korea Republic': { fr: 'Corée du Sud', code: 'kr' },
    'Republic of Korea': { fr: 'Corée du Sud', code: 'kr' },
    Korea: { fr: 'Corée du Sud', code: 'kr' },
    Czechia: { fr: 'Tchéquie', code: 'cz' },
    'Czech Republic': { fr: 'Tchéquie', code: 'cz' },
    Canada: { fr: 'Canada', code: 'ca' },
    'Bosnia-Herzegovina': { fr: 'Bosnie-Herzégovine', code: 'ba' },
    'Bosnia and Herzegovina': { fr: 'Bosnie-Herzégovine', code: 'ba' },
    'Bosnia & Herzegovina': { fr: 'Bosnie-Herzégovine', code: 'ba' },
    Qatar: { fr: 'Qatar', code: 'qa' },
    Switzerland: { fr: 'Suisse', code: 'ch' },
    Brazil: { fr: 'Brésil', code: 'br' },
    Morocco: { fr: 'Maroc', code: 'ma' },
    Haiti: { fr: 'Haïti', code: 'ht' },
    Scotland: { fr: 'Écosse', code: 'gb-sct' },
    'United States': { fr: 'États-Unis', code: 'us' },
    'United States of America': { fr: 'États-Unis', code: 'us' },
    'The United States': { fr: 'États-Unis', code: 'us' },
    USA: { fr: 'États-Unis', code: 'us' },
    US: { fr: 'États-Unis', code: 'us' },
    Paraguay: { fr: 'Paraguay', code: 'py' },
    Australia: { fr: 'Australie', code: 'au' },
    Turkey: { fr: 'Turquie', code: 'tr' },
    Germany: { fr: 'Allemagne', code: 'de' },
    Curaçao: { fr: 'Curaçao', code: 'cw' },
    Curacao: { fr: 'Curaçao', code: 'cw' },
    "Côte d'Ivoire": { fr: "Côte d'Ivoire", code: 'ci' },
    'Ivory Coast': { fr: "Côte d'Ivoire", code: 'ci' },
    Ecuador: { fr: 'Équateur', code: 'ec' },
    Netherlands: { fr: 'Pays-Bas', code: 'nl' },
    Holland: { fr: 'Pays-Bas', code: 'nl' },
    Japan: { fr: 'Japon', code: 'jp' },
    Sweden: { fr: 'Suède', code: 'se' },
    Tunisia: { fr: 'Tunisie', code: 'tn' },
    Belgium: { fr: 'Belgique', code: 'be' },
    Egypt: { fr: 'Égypte', code: 'eg' },
    Iran: { fr: 'Iran', code: 'ir' },
    'New Zealand': { fr: 'Nouvelle-Zélande', code: 'nz' },
    'Aotearoa New Zealand': { fr: 'Nouvelle-Zélande', code: 'nz' },
    Spain: { fr: 'Espagne', code: 'es' },
    'Cape Verde Islands': { fr: 'Cap-Vert', code: 'cv' },
    'Cape Verde': { fr: 'Cap-Vert', code: 'cv' },
    'Cabo Verde': { fr: 'Cap-Vert', code: 'cv' },
    'República de Cabo Verde': { fr: 'Cap-Vert', code: 'cv' },
    'Saudi Arabia': { fr: 'Arabie Saoudite', code: 'sa' },
    KSA: { fr: 'Arabie Saoudite', code: 'sa' },
    Uruguay: { fr: 'Uruguay', code: 'uy' },
    France: { fr: 'France', code: 'fr' },
    Senegal: { fr: 'Sénégal', code: 'sn' },
    Iraq: { fr: 'Irak', code: 'iq' },
    Norway: { fr: 'Norvège', code: 'no' },
    Argentina: { fr: 'Argentine', code: 'ar' },
    Algeria: { fr: 'Algérie', code: 'dz' },
    Austria: { fr: 'Autriche', code: 'at' },
    Jordan: { fr: 'Jordanie', code: 'jo' },
    Portugal: { fr: 'Portugal', code: 'pt' },
    'Congo DR': { fr: 'RD Congo', code: 'cd' },
    'DR Congo': { fr: 'RD Congo', code: 'cd' },
    'Democratic Republic of the Congo': { fr: 'RD Congo', code: 'cd' },
    'Democratic Republic of Congo': { fr: 'RD Congo', code: 'cd' },
    DRC: { fr: 'RD Congo', code: 'cd' },
    RDC: { fr: 'RD Congo', code: 'cd' },
    'Congo-Kinshasa': { fr: 'RD Congo', code: 'cd' },
    Uzbekistan: { fr: 'Ouzbékistan', code: 'uz' },
    Colombia: { fr: 'Colombie', code: 'co' },
    England: { fr: 'Angleterre', code: 'gb-eng' },
    Croatia: { fr: 'Croatie', code: 'hr' },
    Ghana: { fr: 'Ghana', code: 'gh' },
    Panama: { fr: 'Panama', code: 'pa' },
    Mexique: { fr: 'Mexique', code: 'mx' },
    'Afrique du Sud': { fr: 'Afrique du Sud', code: 'za' },
    'Corée du Sud': { fr: 'Corée du Sud', code: 'kr' },
    Tchéquie: { fr: 'Tchéquie', code: 'cz' },
    'Bosnie-Herzégovine': { fr: 'Bosnie-Herzégovine', code: 'ba' },
    Suisse: { fr: 'Suisse', code: 'ch' },
    Brésil: { fr: 'Brésil', code: 'br' },
    Maroc: { fr: 'Maroc', code: 'ma' },
    Haïti: { fr: 'Haïti', code: 'ht' },
    Écosse: { fr: 'Écosse', code: 'gb-sct' },
    'États-Unis': { fr: 'États-Unis', code: 'us' },
    Paraguay: { fr: 'Paraguay', code: 'py' },
    Australie: { fr: 'Australie', code: 'au' },
    Turquie: { fr: 'Turquie', code: 'tr' },
    Allemagne: { fr: 'Allemagne', code: 'de' },
    Équateur: { fr: 'Équateur', code: 'ec' },
    'Pays-Bas': { fr: 'Pays-Bas', code: 'nl' },
    Japon: { fr: 'Japon', code: 'jp' },
    Suède: { fr: 'Suède', code: 'se' },
    Tunisie: { fr: 'Tunisie', code: 'tn' },
    Belgique: { fr: 'Belgique', code: 'be' },
    Égypte: { fr: 'Égypte', code: 'eg' },
    'Nouvelle-Zélande': { fr: 'Nouvelle-Zélande', code: 'nz' },
    Espagne: { fr: 'Espagne', code: 'es' },
    'Cap-Vert': { fr: 'Cap-Vert', code: 'cv' },
    'Arabie Saoudite': { fr: 'Arabie Saoudite', code: 'sa' },
    Sénégal: { fr: 'Sénégal', code: 'sn' },
    Irak: { fr: 'Irak', code: 'iq' },
    Norvège: { fr: 'Norvège', code: 'no' },
    Argentine: { fr: 'Argentine', code: 'ar' },
    Algérie: { fr: 'Algérie', code: 'dz' },
    Autriche: { fr: 'Autriche', code: 'at' },
    Jordanie: { fr: 'Jordanie', code: 'jo' },
    'RD Congo': { fr: 'RD Congo', code: 'cd' },
    Ouzbékistan: { fr: 'Ouzbékistan', code: 'uz' },
    Colombie: { fr: 'Colombie', code: 'co' },
    Angleterre: { fr: 'Angleterre', code: 'gb-eng' },
    Croatie: { fr: 'Croatie', code: 'hr' }
  };
function normalizeTeamName(e) {
  return e && 'TBD' !== e ? String(e).trim() : e;
}
function translateTeamName(e) {
  const a = normalizeTeamName(e),
    t = TEAM_TRANSLATIONS[a];
  return t ? t.fr : a;
}
function teamFlagCode(e) {
  const a = normalizeTeamName(e),
    t = TEAM_TRANSLATIONS[a];
  return t ? t.code : 'xx';
}
const STATIC_FALLBACK_MATCHES = {
    2026: [
      {
        id: 537327,
        homeTeam: { name: 'Mexique', shortName: 'MEX', tla: 'MEX' },
        awayTeam: { name: 'Afrique du Sud', shortName: 'RSA', tla: 'RSA' },
        score: { fullTime: { home: 2, away: 0 } },
        status: 'FINISHED',
        utcDate: '2026-06-11T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe A',
        venue: 'Estadio Azteca'
      },
      {
        id: 537328,
        homeTeam: { name: 'Corée du Sud', shortName: 'KOR', tla: 'KOR' },
        awayTeam: { name: 'Tchéquie', shortName: 'CZE', tla: 'CZE' },
        score: { fullTime: { home: 2, away: 1 } },
        status: 'FINISHED',
        utcDate: '2026-06-12T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe A',
        venue: 'Estadio BBVA'
      },
      {
        id: 537325,
        homeTeam: { name: 'Tchéquie', shortName: 'CZE', tla: 'CZE' },
        awayTeam: { name: 'Mexique', shortName: 'MEX', tla: 'MEX' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-18T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe A',
        venue: 'AT&T Stadium'
      },
      {
        id: 537326,
        homeTeam: { name: 'Afrique du Sud', shortName: 'RSA', tla: 'RSA' },
        awayTeam: { name: 'Corée du Sud', shortName: 'KOR', tla: 'KOR' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-18T22:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe A',
        venue: 'Lumen Field'
      },
      {
        id: 537329,
        homeTeam: { name: 'Mexique', shortName: 'MEX', tla: 'MEX' },
        awayTeam: { name: 'Corée du Sud', shortName: 'KOR', tla: 'KOR' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-19T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe A',
        venue: 'Mercedes-Benz Stadium'
      },
      {
        id: 537330,
        homeTeam: { name: 'Afrique du Sud', shortName: 'RSA', tla: 'RSA' },
        awayTeam: { name: 'Tchéquie', shortName: 'CZE', tla: 'CZE' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-19T22:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe A',
        venue: 'Gillette Stadium'
      },
      {
        id: 537333,
        homeTeam: { name: 'Canada', shortName: 'CAN', tla: 'CAN' },
        awayTeam: { name: 'Bosnie-Herzégovine', shortName: 'BIH', tla: 'BIH' },
        score: { fullTime: { home: 1, away: 1 } },
        status: 'FINISHED',
        utcDate: '2026-06-12T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe B',
        venue: 'BC Place'
      },
      {
        id: 537332,
        homeTeam: { name: 'Qatar', shortName: 'QAT', tla: 'QAT' },
        awayTeam: { name: 'Suisse', shortName: 'SUI', tla: 'SUI' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-13T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe B',
        venue: 'Hard Rock Stadium'
      },
      {
        id: 537335,
        homeTeam: { name: 'Suisse', shortName: 'SUI', tla: 'SUI' },
        awayTeam: { name: 'Bosnie-Herzégovine', shortName: 'BIH', tla: 'BIH' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-18T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe B',
        venue: "Levi's Stadium"
      },
      {
        id: 537336,
        homeTeam: { name: 'Canada', shortName: 'CAN', tla: 'CAN' },
        awayTeam: { name: 'Qatar', shortName: 'QAT', tla: 'QAT' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-18T22:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe B',
        venue: 'BMO Field'
      },
      {
        id: 537337,
        homeTeam: { name: 'Brésil', shortName: 'BRA', tla: 'BRA' },
        awayTeam: { name: 'Maroc', shortName: 'MAR', tla: 'MAR' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-13T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe C',
        venue: 'MetLife Stadium'
      },
      {
        id: 537338,
        homeTeam: { name: 'Haïti', shortName: 'HAI', tla: 'HAI' },
        awayTeam: { name: 'Écosse', shortName: 'SCO', tla: 'SCO' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-14T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe C',
        venue: 'NRG Stadium'
      },
      {
        id: 537345,
        homeTeam: { name: 'United States', shortName: 'USA', tla: 'USA' },
        awayTeam: { name: 'Paraguay', shortName: 'PAR', tla: 'PAR' },
        score: { fullTime: { home: 4, away: 1 } },
        status: 'FINISHED',
        utcDate: '2026-06-13T01:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe D',
        venue: 'SoFi Stadium'
      },
      {
        id: 537346,
        homeTeam: { name: 'Australia', shortName: 'AUS', tla: 'AUS' },
        awayTeam: { name: 'Turkey', shortName: 'TUR', tla: 'TUR' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-14T04:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe D',
        venue: 'AT&T Stadium'
      },
      {
        id: 537343,
        homeTeam: { name: 'Allemagne', shortName: 'GER', tla: 'GER' },
        awayTeam: { name: 'Curaçao', shortName: 'CUW', tla: 'CUW' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-14T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe E',
        venue: 'Lincoln Financial Field'
      },
      {
        id: 537344,
        homeTeam: { name: "Côte d'Ivoire", shortName: 'CIV', tla: 'CIV' },
        awayTeam: { name: 'Équateur', shortName: 'ECU', tla: 'ECU' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-14T22:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe E',
        venue: 'Arrowhead Stadium'
      },
      {
        id: 537347,
        homeTeam: { name: 'Pays-Bas', shortName: 'NED', tla: 'NED' },
        awayTeam: { name: 'Japon', shortName: 'JPN', tla: 'JPN' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-14T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe F',
        venue: 'Mercedes-Benz Stadium'
      },
      {
        id: 537348,
        homeTeam: { name: 'Suède', shortName: 'SWE', tla: 'SWE' },
        awayTeam: { name: 'Tunisie', shortName: 'TUN', tla: 'TUN' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-15T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe F',
        venue: 'Gillette Stadium'
      },
      {
        id: 537351,
        homeTeam: { name: 'Belgique', shortName: 'BEL', tla: 'BEL' },
        awayTeam: { name: 'Égypte', shortName: 'EGY', tla: 'EGY' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-15T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe G',
        venue: 'MetLife Stadium'
      },
      {
        id: 537352,
        homeTeam: { name: 'Iran', shortName: 'IRN', tla: 'IRN' },
        awayTeam: { name: 'Nouvelle-Zélande', shortName: 'NZL', tla: 'NZL' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-16T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe G',
        venue: 'Lumen Field'
      },
      {
        id: 537355,
        homeTeam: { name: 'Espagne', shortName: 'ESP', tla: 'ESP' },
        awayTeam: { name: 'Cap-Vert', shortName: 'CPV', tla: 'CPV' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-15T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe H',
        venue: 'Hard Rock Stadium'
      },
      {
        id: 537356,
        homeTeam: { name: 'Arabie Saoudite', shortName: 'KSA', tla: 'KSA' },
        awayTeam: { name: 'Uruguay', shortName: 'URU', tla: 'URU' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-15T22:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe H',
        venue: 'AT&T Stadium'
      },
      {
        id: 537359,
        homeTeam: { name: 'France', shortName: 'FRA', tla: 'FRA' },
        awayTeam: { name: 'Sénégal', shortName: 'SEN', tla: 'SEN' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-16T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe I',
        venue: "Levi's Stadium"
      },
      {
        id: 537360,
        homeTeam: { name: 'Irak', shortName: 'IRQ', tla: 'IRQ' },
        awayTeam: { name: 'Norvège', shortName: 'NOR', tla: 'NOR' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-16T22:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe I',
        venue: 'BC Place'
      },
      {
        id: 537363,
        homeTeam: { name: 'Argentine', shortName: 'ARG', tla: 'ARG' },
        awayTeam: { name: 'Algérie', shortName: 'ALG', tla: 'ALG' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-17T01:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe J',
        venue: 'Estadio Azteca'
      },
      {
        id: 537364,
        homeTeam: { name: 'Autriche', shortName: 'AUT', tla: 'AUT' },
        awayTeam: { name: 'Jordanie', shortName: 'JOR', tla: 'JOR' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-17T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe J',
        venue: 'BMO Field'
      },
      {
        id: 537367,
        homeTeam: { name: 'Portugal', shortName: 'POR', tla: 'POR' },
        awayTeam: { name: 'RD Congo', shortName: 'COD', tla: 'COD' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-17T19:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe K',
        venue: 'SoFi Stadium'
      },
      {
        id: 537368,
        homeTeam: { name: 'Ouzbékistan', shortName: 'UZB', tla: 'UZB' },
        awayTeam: { name: 'Colombie', shortName: 'COL', tla: 'COL' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-18T01:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe K',
        venue: 'NRG Stadium'
      },
      {
        id: 537371,
        homeTeam: { name: 'Angleterre', shortName: 'ENG', tla: 'ENG' },
        awayTeam: { name: 'Croatie', shortName: 'CRO', tla: 'CRO' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-17T01:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe L',
        venue: 'Gillette Stadium'
      },
      {
        id: 537372,
        homeTeam: { name: 'Ghana', shortName: 'GHA', tla: 'GHA' },
        awayTeam: { name: 'Panama', shortName: 'PAN', tla: 'PAN' },
        score: { fullTime: { home: null, away: null } },
        status: 'SCHEDULED',
        utcDate: '2026-06-17T02:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe L',
        venue: 'Lumen Field'
      }
    ],
    2022: [
      {
        id: 101,
        homeTeam: { name: 'Argentine', shortName: 'ARG', tla: 'ARG' },
        awayTeam: { name: 'France', shortName: 'FRA', tla: 'FRA' },
        score: { fullTime: { home: 3, away: 3 } },
        status: 'FINISHED',
        utcDate: '2022-12-18T15:00:00Z',
        stage: 'FINAL',
        venue: 'Lusail Stadium'
      },
      {
        id: 102,
        homeTeam: { name: 'Argentine', shortName: 'ARG', tla: 'ARG' },
        awayTeam: { name: 'Croatie', shortName: 'CRO', tla: 'CRO' },
        score: { fullTime: { home: 3, away: 0 } },
        status: 'FINISHED',
        utcDate: '2022-12-13T19:00:00Z',
        stage: 'SEMI_FINALS',
        venue: 'Lusail Stadium'
      },
      {
        id: 103,
        homeTeam: { name: 'France', shortName: 'FRA', tla: 'FRA' },
        awayTeam: { name: 'Maroc', shortName: 'MAR', tla: 'MAR' },
        score: { fullTime: { home: 2, away: 0 } },
        status: 'FINISHED',
        utcDate: '2022-12-14T19:00:00Z',
        stage: 'SEMI_FINALS',
        venue: 'Al Bayt Stadium'
      },
      {
        id: 104,
        homeTeam: { name: 'Pays-Bas', shortName: 'NED', tla: 'NED' },
        awayTeam: { name: 'Argentine', shortName: 'ARG', tla: 'ARG' },
        score: { fullTime: { home: 2, away: 2 } },
        status: 'FINISHED',
        utcDate: '2022-12-09T19:00:00Z',
        stage: 'QUARTER_FINALS',
        venue: 'Lusail Stadium'
      },
      {
        id: 105,
        homeTeam: { name: 'Arabie Saoudite', shortName: 'KSA', tla: 'KSA' },
        awayTeam: { name: 'Argentine', shortName: 'ARG', tla: 'ARG' },
        score: { fullTime: { home: 2, away: 1 } },
        status: 'FINISHED',
        utcDate: '2022-11-22T10:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe C',
        venue: 'Lusail Stadium'
      },
      {
        id: 106,
        homeTeam: { name: 'Japon', shortName: 'JPN', tla: 'JPN' },
        awayTeam: { name: 'Allemagne', shortName: 'GER', tla: 'GER' },
        score: { fullTime: { home: 2, away: 1 } },
        status: 'FINISHED',
        utcDate: '2022-11-23T13:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe E',
        venue: 'Khalifa International Stadium'
      },
      {
        id: 107,
        homeTeam: { name: 'Maroc', shortName: 'MAR', tla: 'MAR' },
        awayTeam: { name: 'Belgique', shortName: 'BEL', tla: 'BEL' },
        score: { fullTime: { home: 2, away: 0 } },
        status: 'FINISHED',
        utcDate: '2022-11-27T13:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe F',
        venue: 'Al Thumama Stadium'
      },
      {
        id: 108,
        homeTeam: { name: 'Corée du Sud', shortName: 'KOR', tla: 'KOR' },
        awayTeam: { name: 'Portugal', shortName: 'POR', tla: 'POR' },
        score: { fullTime: { home: 2, away: 1 } },
        status: 'FINISHED',
        utcDate: '2022-12-02T15:00:00Z',
        stage: 'GROUP_STAGE',
        group: 'Groupe H',
        venue: 'Education City Stadium'
      }
    ]
  },
  STATIC_FALLBACK_GOALS = {
    537327: [
      { minute: 9, scorer: 'J. Quiñones', team: 'home' },
      { minute: 67, scorer: 'R. Jiménez', team: 'home' }
    ],
    537328: [
      { minute: 67, scorer: 'I.B. Hwang', team: 'home' },
      { minute: 80, scorer: 'H.G. Oh', team: 'home' },
      { minute: 59, scorer: 'L. Krejčí', team: 'away' }
    ],
    537333: [
      { minute: 11, scorer: 'C. Larin', team: 'home' },
      { minute: 21, scorer: 'Jovo Lukić', team: 'away' }
    ],
    537345: [
      { minute: 7, scorer: 'D. Bobadilla (OG)', team: 'home' },
      { minute: 31, scorer: 'F. Balogun', team: 'home' },
      { minute: '45+5', scorer: 'F. Balogun', team: 'home' },
      { minute: '90+8', scorer: 'G. Reyna', team: 'home' },
      { minute: 73, scorer: 'Maurício', team: 'away' }
    ]
  };
let previousScores = new Map(),
  lastRefreshTime = new Map(),
  chartMatches = [],
  wc26Scorers = new Map();
function resetComputeCache() {
  computeCache = { status: new Map(), scorers: new Map(), flagCode: new Map() };
}
function cacheKeyForMatch(e) {
  return `${e.id}|${e.status}|${e.finished}|${e.time_elapsed}|${e.minute}|${e.utcDate}|${e.score?.fullTime?.home ?? ''}|${e.score?.fullTime?.away ?? ''}`;
}
function getMatchEffectiveStatusCached(e) {
  computeCache.status || resetComputeCache();
  const a = cacheKeyForMatch(e);
  if (computeCache.status.has(a)) return computeCache.status.get(a);
  const t = getMatchEffectiveStatus(e);
  return (computeCache.status.set(a, t), t);
}
function getGoalScorersCached(e) {
  computeCache.scorers || resetComputeCache();
  const a = cacheKeyForMatch(e);
  if (computeCache.scorers.has(a)) return computeCache.scorers.get(a);
  const t = getGoalScorers(e);
  return (computeCache.scorers.set(a, t), t);
}
function flagCodeForTeamCached(e) {
  if (!e || 'TBD' === e) return 'xx';
  const a = String(e);
  if (computeCache.flagCode.has(a)) return computeCache.flagCode.get(a);
  const t = flagCodeForTeam(e);
  return (computeCache.flagCode.set(a, t), t);
}
function flagForTeamCached(e) {
  const a = flagCodeForTeamCached(e);
  return 'xx' === a
    ? '<span class="flag-placeholder" aria-hidden="true"></span>'
    : `<img src="https://flagcdn.com/${a}.svg" alt="" loading="lazy">`;
}
function cleanupMatchMaps(e) {
  const a = new Set();
  e.forEach(e => {
    (a.add(`${e.id}-home`), a.add(`${e.id}-away`), a.add(e.id));
  });
  for (const e of previousScores.keys()) a.has(e) || previousScores.delete(e);
  for (const e of lastRefreshTime.keys()) a.has(e) || lastRefreshTime.delete(e);
}
async function fetchWorldcup26Scorers({ preferFresh: e = !1 } = {}) {
  const a = getWc26Cache({ maxAgeMs: e ? 0 : 3e4 });
  a && a.matches && parseWcup2026Payload(a);
}
function extractScorerItems(e) {
  if (!e || 'null' === e) return [];
  const a = String(e).replace(/[“”]/g, '"').replace(/[‘’]/g, "'").replace(/^{/, '[').replace(/}$/, ']');
  try {
    const e = JSON.parse(a);
    if (Array.isArray(e)) return e.filter(e => e && String(e).trim());
  } catch (e) {}
  const t = a.match(/"([^"]*)"/g);
  return t
    ? t.map(e => e.replace(/^["']|["']$/g, '')).filter(Boolean)
    : a
        .split(',')
        .map(e => e.trim().replace(/^["']|["']$/g, ''))
        .filter(Boolean);
}
function parseScorerString(e) {
  const a = String(e).trim();
  if (!a) return null;
  const t = a.match(/^(.+?)\s+(\d+(?:\s*\+\s*\d+)?)\s*(?:'|")?\s*(.*)$/);
  if (t) {
    let e = t[1].trim(),
      a = t[2].replace(/\s+/g, '').replace(/['"]/g, ''),
      r = t[3].trim();
    const o = r.match(/^(\+\d+)\s*(?:'|")?\s*(.*)$/);
    return (o && ((a += o[1]), (r = o[2].trim())), { scorer: e + (r ? ' ' + r : ''), minute: a });
  }
  return { scorer: a, minute: '' };
}
function normalizeScorerName(e) {
  return e
    ? String(e)
        .toLowerCase()
        .normalize('NFD')
        .replace(/[̀-ͯ]/g, '')
        .replace(/\s+/g, ' ')
        .replace(/\s*\([^)]+\)\s*$/g, '')
        .replace(/\s*\+\d+['"]?\s*$/g, '')
        .replace(/[^a-z0-9 ]/g, '')
        .trim()
    : '';
}
function isBetterScorerDisplay(e, a) {
  if (!a) return !0;
  const t = /\d|'|"|\(\w+\)|\+/,
    r = t.test(e),
    o = t.test(a);
  return !(!o || r) || (!(!o && r) && e.length > a.length);
}
function isOwnGoalFromData(e) {
  if (!e) return !1;
  return !!(e.ownGoal || e.owngoal || e.own_goal || /\(OG\)|\(CSC\)/i.test(String(e.scorer || e.name || '')));
}
function formatScorerName(e, a) {
  let t = String(e || '').trim();
  const r = /\(OG\)|\(CSC\)/i.test(t);
  a = a || r;
  t = t.replace(/\s*\(OG\)\s*/gi, '').replace(/\s*\(CSC\)\s*/gi, '').trim();
  return a && !t.includes('(CSC)') ? `${t} (CSC)` : t;
}
function getScorersForMatch(e) {
  const a = e.homeTeam?.name,
    t = e.awayTeam?.name;
  if (!a || !t) return [];
  const r = `${a}|${t}`,
    o = wc26Scorers.get(r);
  return o && o.length
    ? o
        .map(e => {
          const r = isOwnGoalFromData(e);
          if ('string' == typeof e.scorer) return { minute: e.minute ?? '', scorer: formatScorerName(e.scorer, r), side: e.side, ownGoal: r };
          const a = parseScorerString(e.raw);
          if (!a) return null;
          const t = isOwnGoalFromData(e) || isOwnGoalFromData(a);
          return { minute: a.minute, scorer: formatScorerName(a.scorer, t), side: e.side, ownGoal: t };
        })
        .filter(e => e && e.scorer && 'undefined' !== e.scorer)
    : e.goals && e.goals.length
      ? e.goals
          .map(e => {
            const r = isOwnGoalFromData(e),
              o =
                'string' == typeof e.scorer
                  ? e.side || ((e.team?.name ?? e.team) === a ? 'home' : 'away')
                  : (e.team?.name ?? e.team) === a
                    ? 'home'
                    : 'away';
            return { minute: e.minute ?? e.time ?? '', scorer: formatScorerName('string' == typeof e.scorer ? e.scorer : e.scorer?.name ?? e.player?.name ?? '', r), side: o, ownGoal: r };
          })
          .filter(e => e.scorer && 'undefined' !== e.scorer)
      : (STATIC_FALLBACK_GOALS[e.id] || [])
          .map(e => {
            const a = isOwnGoalFromData(e);
            return { minute: e.minute, scorer: formatScorerName(e.scorer, a), side: e.team, ownGoal: a };
          })
          .filter(e => e.scorer && 'undefined' !== e.scorer);
}
function getWc26Cache({ maxAgeMs: e = 3e4 } = {}) {
  try {
    const a = localStorage.getItem('wcup2026-cache-v2');
    if (!a) return null;
    const t = JSON.parse(a);
    return Date.now() - t.ts > e ? null : t.data;
  } catch (e) {
    return null;
  }
}
function setWc26Cache(e) {
  try {
    (localStorage.setItem('wcup2026-cache-v2', JSON.stringify({ ts: Date.now(), data: e })),
      localStorage.removeItem('wcup2026-cache'),
      localStorage.removeItem('wc26-cache'));
  } catch (e) {}
}
async function fetchWithTimeout(e, a, t = 8e3) {
  const r = new AbortController(),
    o = setTimeout(() => r.abort(new Error('timeout')), t);
  try {
    const t = await fetch(e, { ...a, signal: r.signal });
    return (clearTimeout(o), t);
  } catch (e) {
    if ((clearTimeout(o), 'AbortError' === e.name)) throw new Error(`timeout after ${t}ms`);
    throw e;
  }
}
async function fetchWorldcup26Matches({ preferFresh: e = !1 } = {}) {
  const a = getWc26Cache({ maxAgeMs: e ? 0 : 3e4 });
  if (wc26ApiFailures >= 3 && a && a.matches && Array.isArray(a.matches))
    return ((lastWc26FetchSource = 'cache'), parseWcup2026Payload(a));
  if (!e && a && a.matches && Array.isArray(a.matches))
    return ((lastWc26FetchSource = 'cache'), parseWcup2026Payload(a));
  if (wc26FetchPromise) return wc26FetchPromise;
  wc26FetchPromise = (async () => {
    const t = '/api/wcup2026-proxy.php' + (e ? '?force=1' : '');
    let r = null;
    for (let e = 0; e < 2; e++)
      try {
        e > 0 && (await new Promise(e => setTimeout(e, 500)));
        const a = await fetchWithTimeout(t, { cache: 'no-store' }, 8e3);
        if (!a.ok) {
          if (a.status >= 500 && a.status < 600) throw new Error(`server error ${a.status}`);
          throw new Error(`HTTP ${a.status}`);
        }
        const r = await a.json();
        if (!r.matches || !Array.isArray(r.matches)) throw new Error('invalid payload');
        return (
          setWc26Cache(r),
          (wc26ApiFailures = 0),
          (wc26FirstFetchDone = !0),
          (lastWc26FetchSource = 'network'),
          parseWcup2026Payload(r)
        );
      } catch (a) {
        if (
          ((r = a),
          String(a.message).startsWith('timeout after')
            ? console.warn(`[wcup2026] fetch matches attempt ${e + 1} timed out`)
            : console.warn(`[wcup2026] fetch matches attempt ${e + 1} failed:`, a.message || a),
          String(a.message).startsWith('server error'))
        )
          break;
      }
    return (
      (wc26ApiFailures += 1),
      (wc26FirstFetchDone = !0),
      a && a.matches
        ? (console.warn('[wcup2026] using stale cache after network failure'),
          (lastWc26FetchSource = 'stale-cache'),
          parseWcup2026Payload(a))
        : ((lastWc26FetchSource = 'none'), console.error('[wcup2026] fetch matches failed:', r?.message || r), null)
    );
  })();
  try {
    return await wc26FetchPromise;
  } finally {
    wc26FetchPromise = null;
  }
}
function parseWorldcup26Payload(e) {
  try {
    if (!e.games || !Array.isArray(e.games)) return null;
    const a = new Map(),
      t = e.games.map(e => {
        const t = translateTeamName(e.home_team_name_en || 'TBD'),
          r = translateTeamName(e.away_team_name_en || 'TBD'),
          o = STADIUM_ID_TO_NAME[String(e.stadium_id)] || 'Stade à confirmer',
          n = parseWc26DateToIso(e.local_date, e.stadium_id) || new Date().toISOString(),
          s = mapWc26TypeToStage(e.type),
          c = (e.group || '').toString().trim(),
          i = 'GROUP_STAGE' === s && c ? `Groupe ${c.toUpperCase()}` : c,
          l = e.home_score,
          u = e.away_score,
          m = 'TRUE' === (e.finished || '').toString().toUpperCase(),
          d = (e.time_elapsed || '').toString().toLowerCase(),
          h = !m && 'notstarted' === d,
          p = h || null == l || '' === l ? null : Number(l),
          g = h || null == u || '' === u ? null : Number(u);
        let f = 'SCHEDULED';
        m ? (f = 'FINISHED') : ('live' === d || (!isNaN(Number(d)) && '' !== d)) && (f = 'IN_PLAY');
        const S = [],
          T = (e, a) => {
            e &&
              'null' !== e &&
              extractScorerItems(String(e)).forEach(e => {
                const o = parseScorerString(e);
                o && S.push({ minute: o.minute, scorer: o.scorer, team: { name: 'home' === a ? t : r }, side: a });
              });
          };
        return (
          T(e.home_scorers, 'home'),
          T(e.away_scorers, 'away'),
          a.set(`${t}|${r}`, S),
          {
            id: e.id || `${t}-${r}`,
            homeTeam: { name: t, shortName: t.slice(0, 3).toUpperCase(), tla: t.slice(0, 3).toUpperCase() },
            awayTeam: { name: r, shortName: r.slice(0, 3).toUpperCase(), tla: r.slice(0, 3).toUpperCase() },
            score: { fullTime: { home: p, away: g }, halfTime: { home: null, away: null } },
            goals: S,
            status: f,
            finished: m,
            time_elapsed: d,
            utcDate: n,
            stage: s,
            group: i,
            venue: o
          }
        );
      });
    return ((wc26Scorers = a), t);
  } catch (e) {
    return (console.warn('[worldcup26] parse payload failed:', e), null);
  }
}
function parseWcup2026Payload(e) {
  try {
    if (!e.matches || !Array.isArray(e.matches)) return null;
    const a = new Map(),
      t = e.matches.map((e, t) => {
        const r = translateTeamName(e.team1 || 'TBD'),
          o = translateTeamName(e.team2 || 'TBD'),
          n = (e.ground || '').toString().trim(),
          s = WCUP2026_GROUND_TO_STADIUM[n] || n || 'Stade à confirmer',
          c = e.datetime ? new Date(1e3 * e.datetime).toISOString() : new Date().toISOString(),
          i = (e.status || '').toString().toLowerCase(),
          l = 'finished' === i,
          u = 'live' === i;
        let m = 'SCHEDULED';
        l ? (m = 'FINISHED') : u && (m = 'IN_PLAY');
        const d = Array.isArray(e.score) ? e.score : [null, null],
          h = null === d[0] || void 0 === d[0] || isNaN(Number(d[0])) ? null : Number(d[0]),
          p = null === d[1] || void 0 === d[1] || isNaN(Number(d[1])) ? null : Number(d[1]),
          g = [],
          f = (e, a) => {
            if (!e || !e.name) return;
            const t = String(e.minute ?? '').replace(/\s+/g, '');
            g.push({ minute: t, scorer: e.name, team: { name: 'home' === a ? r : o }, side: a });
          };
        ((e.goals1 || []).forEach(e => f(e, 'home')),
          (e.goals2 || []).forEach(e => f(e, 'away')),
          a.set(`${r}|${o}`, g));
        const S = (e.round || '').toString().toLowerCase(),
          T = (e.group || '')
            .toString()
            .replace(/^Group\s*/i, '')
            .trim(),
          C = T ? `Groupe ${T.toUpperCase()}` : '';
        let A = 'GROUP_STAGE';
        return (
          S.includes('round of 32')
            ? (A = 'ROUND_OF_32')
            : S.includes('round of 16')
              ? (A = 'ROUND_OF_16')
              : S.includes('quarter')
                ? (A = 'QUARTER_FINALS')
                : S.includes('semi')
                  ? (A = 'SEMI_FINALS')
                  : S.includes('third')
                    ? (A = 'THIRD_PLACE')
                    : S.includes('final') && (A = 'FINAL'),
          {
            id: void 0 !== e.id ? e.id : t,
            homeTeam: { name: r, shortName: r.slice(0, 3).toUpperCase(), tla: r.slice(0, 3).toUpperCase() },
            awayTeam: { name: o, shortName: o.slice(0, 3).toUpperCase(), tla: o.slice(0, 3).toUpperCase() },
            score: { fullTime: { home: h, away: p }, halfTime: { home: null, away: null } },
            goals: g,
            status: m,
            finished: l,
            time_elapsed: u ? String(e.live_minute || 'live') : l ? 'finished' : 'notstarted',
            minute: e.live_minute || '',
            utcDate: c,
            stage: A,
            group: C,
            venue: s
          }
        );
      });
    return ((wc26Scorers = a), t);
  } catch (e) {
    return (console.warn('[wcup2026] parse payload failed:', e), null);
  }
}
function toParisTime(e) {
  try {
    return new Intl.DateTimeFormat('fr-FR', {
      timeZone: 'Europe/Paris',
      hour: '2-digit',
      minute: '2-digit',
      day: '2-digit',
      month: '2-digit',
      year: 'numeric'
    }).format(new Date(e));
  } catch (a) {
    return e;
  }
}
function toLocalTime(e, a) {
  try {
    const t = STADIUM_TIMEZONES[a] || 'UTC';
    return new Intl.DateTimeFormat('fr-FR', { timeZone: t, hour: '2-digit', minute: '2-digit' }).format(new Date(e));
  } catch (e) {
    return '--:--';
  }
}
function parseWc26DateToIso(e, a) {
  const t = (e || '').match(/^(\d{2})\/(\d{2})\/(\d{4})\s+(\d{2}):(\d{2})$/);
  if (!t) return null;
  const [, r, o, n, s, c] = t,
    i = STADIUM_ID_TO_NAME[String(a)] || 'UTC',
    l = STADIUM_TIMEZONES[i] || 'UTC',
    u = new Date(Date.UTC(+n, +r - 1, +o, +s, +c)),
    m = new Intl.DateTimeFormat('en-US', {
      timeZone: l,
      year: 'numeric',
      month: '2-digit',
      day: '2-digit',
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit',
      hour12: !1
    })
      .format(u)
      .match(/^(\d{2})\/(\d{2})\/(\d{4}),\s+(\d{2}):(\d{2}):(\d{2})$/);
  if (!m) return u.toISOString();
  const [, d, h, p, g, f] = m,
    S = new Date(+p, +d - 1, +h, +g, +f).getTime(),
    T = new Date(+n, +r - 1, +o, +s, +c).getTime() - S;
  return new Date(u.getTime() + T).toISOString();
}
function formatMatchDateTime(e, a) {
  try {
    const t = new Date(e),
      r = STADIUM_TIMEZONES[a] || 'UTC';
    return {
      local: new Intl.DateTimeFormat('fr-FR', {
        timeZone: r,
        weekday: 'long',
        day: 'numeric',
        month: 'long',
        hour: '2-digit',
        minute: '2-digit'
      }).format(t),
      paris: new Intl.DateTimeFormat('fr-FR', {
        timeZone: 'Europe/Paris',
        weekday: 'long',
        day: 'numeric',
        month: 'long',
        hour: '2-digit',
        minute: '2-digit'
      }).format(t),
      date: t
    };
  } catch (a) {
    return { local: e, paris: e, date: new Date(e) };
  }
}
function getMatchEffectiveStatus(e) {
  if (!0 === e.finished || 'TRUE' === e.finished || 'FINISHED' === e.status || 'AWARDED' === e.status)
    return 'finished';
  const a = (e.time_elapsed || '').toString().toLowerCase();
  if ('finished' === a) return 'finished';
  if (
    (a && 'notstarted' !== a && 'finished' !== a && ('live' === a || !isNaN(Number(a)))) ||
    'IN_PLAY' === e.status ||
    'PAUSED' === e.status ||
    'LIVE' === e.status
  )
    return 'live';
  if ('notstarted' === a) return 'upcoming';
  const t = new Date() - new Date(e.utcDate);
  return t >= 0 && t < 108e5 ? 'live' : t >= 108e5 ? 'finished' : 'upcoming';
}
function formatPhase(e) {
  return (
    {
      GROUP_STAGE: 'Phase de groupes',
      ROUND_OF_32: '32èmes de finale',
      ROUND_OF_16: 'Huitièmes de finale',
      QUARTER_FINALS: 'Quarts de finale',
      SEMI_FINALS: 'Demi-finales',
      THIRD_PLACE: 'Match pour la 3e place',
      FINAL: 'Finale'
    }[e] || e
  );
}
function mapWc26TypeToStage(e) {
  return (
    {
      group: 'GROUP_STAGE',
      r32: 'ROUND_OF_32',
      r16: 'ROUND_OF_16',
      quarter: 'QUARTER_FINALS',
      semi: 'SEMI_FINALS',
      third: 'THIRD_PLACE',
      '3rd': 'THIRD_PLACE',
      final: 'FINAL'
    }[e] ||
    e?.toUpperCase() ||
    'GROUP_STAGE'
  );
}
function getGoalScorers(e) {
  return getScorersForMatch(e);
}
function getStatusLabel(e) {
  return (
    {
      SCHEDULED: 'À venir',
      POSTPONED: 'Reporté',
      CANCELLED: 'Annulé',
      SUSPENDED: 'Interrompu',
      IN_PLAY: 'EN COURS',
      FINISHED: 'Terminé',
      PAUSED: 'Mi-temps',
      AWARDED: 'Attribué'
    }[e] || e
  );
}
function detectActiveWorldCup() {
  const e = new Date(),
    a = WORLD_CUPS.find(a => {
      const t = new Date(a.start),
        r = new Date(a.end);
      return e >= new Date(t.getTime() - 2592e6) && e <= r;
    });
  if (a) {
    const t = e >= new Date(a.start);
    return { ...a, isLive: t, isSoon: !t };
  }
  const t = WORLD_CUPS.filter(a => new Date(a.start) > e).sort((e, a) => new Date(e.start) - new Date(a.start))[0];
  return t ? { ...t, isLive: !1, isSoon: !1 } : WORLD_CUPS[WORLD_CUPS.length - 1];
}
async function autoDiscoverWorldCup() {
  const e = new Date(),
    a = WORLD_CUPS.find(e => 2026 === e.year);
  if (a && e >= new Date(a.start) && e <= new Date(a.end)) return { ...a, isLive: !0, isSoon: !1 };
  try {
    const e = await fetchWithTimeout(
      `https://api.football-data.org/v4/competitions/WC?nocache=${Date.now()}`,
      { headers: { 'X-Auth-Token': API_KEY } },
      5e3
    );
    if (!e.ok) throw new Error(`HTTP ${e.status}`);
    const a = (await e.json()).currentSeason;
    if (!a) return detectActiveWorldCup();
    const t = new Date(a.startDate).getFullYear(),
      r = WORLD_CUPS.findIndex(e => e.year === t);
    return (
      -1 === r
        ? (WORLD_CUPS.push({
            year: t,
            start: a.startDate,
            end: a.endDate,
            hosts: ['TBD'],
            hostNames: 'Détecté automatiquement',
            apiSeasonId: a.id
          }),
          WORLD_CUPS.sort((e, a) => e.year - a.year))
        : ((WORLD_CUPS[r].start = a.startDate), (WORLD_CUPS[r].end = a.endDate), (WORLD_CUPS[r].apiSeasonId = a.id)),
      detectActiveWorldCup()
    );
  } catch (e) {
    return detectActiveWorldCup();
  }
}
function renderTeams(e) {
  const a = document.getElementById('teams-grid');
  if (!a) return;
  const t = {};
  ((STATIC_FALLBACK_TEAMS[e] || STATIC_FALLBACK_TEAMS[2026] || []).forEach(e => {
    const a = e.group || 'Sans groupe';
    (t[a] || (t[a] = []), t[a].push(e));
  }),
    Object.keys(t).forEach(e => t[e].sort((e, a) => e.name.localeCompare(a.name, 'fr'))));
  const r = Object.keys(t).sort((e, a) => e.localeCompare(a, 'fr'));
  a.innerHTML = r
    .map(
      e => (
        e.replace(/\s+/g, '-').toLowerCase(),
        `\n          <div class="group-section" role="group" aria-label="${e}">\n            <h4 style="font-family: var(--font-display); font-size: var(--text-sm); color: var(--color-primary); margin: var(--space-4) 0 var(--space-2); text-transform: uppercase; letter-spacing: 0.05em;">${e}</h4>\n            <div class="grid teams-pc-grid">${t[e].map(e => `<div class="team-chip" role="listitem">\n             <span class="flag" aria-hidden="true"><img src="https://flagcdn.com/${e.code}.svg" alt="${e.name}" loading="lazy"></span>\n             <span>${e.name}</span>\n             <span class="conf">${e.confederation}</span>\n           </div>`).join('')}</div>\n          </div>`
      )
    )
    .join('');
}
function computeGroupStandings(e) {
  const a = {};
  return (
    e.forEach(e => {
      if (!e.group || 'GROUP_STAGE' !== e.stage) return;
      const t = e.group,
        r = e.homeTeam?.name,
        o = e.awayTeam?.name;
      if (!r || !o) return;
      const n = e.score?.fullTime?.home,
        s = e.score?.fullTime?.away,
        c = 'finished' === getMatchEffectiveStatusCached(e);
      (a[t] || (a[t] = {}),
        [r, o].forEach(e => {
          a[t][e] || (a[t][e] = { team: e, played: 0, won: 0, drawn: 0, lost: 0, gf: 0, ga: 0, points: 0 });
        }));
      const i = a[t][r],
        l = a[t][o];
      !c ||
        null == n ||
        isNaN(n) ||
        null == s ||
        isNaN(s) ||
        ((i.played += 1),
        (l.played += 1),
        (i.gf += Number(n)),
        (i.ga += Number(s)),
        (l.gf += Number(s)),
        (l.ga += Number(n)),
        n > s
          ? ((i.won += 1), (i.points += 3), (l.lost += 1))
          : n < s
            ? ((l.won += 1), (l.points += 3), (i.lost += 1))
            : ((i.drawn += 1), (i.points += 1), (l.drawn += 1), (l.points += 1)));
    }),
    Object.fromEntries(
      Object.entries(a).map(([e, a]) => [
        e,
        Object.values(a).sort((e, a) => {
          if (a.points !== e.points) return a.points - e.points;
          const t = e.gf - e.ga,
            r = a.gf - a.ga;
          return r !== t ? r - t : a.gf !== e.gf ? a.gf - e.gf : e.team.localeCompare(a.team, 'fr');
        })
      ])
    )
  );
}
function getKnockoutSlot(e, a, t) {
  const r = e[`Groupe ${a}`];
  return r && r[t - 1]
    ? { type: 'team', label: r[t - 1].team, stats: r[t - 1] }
    : { type: 'placeholder', label: `${1 === t ? '1er' : t + 'ème'} Groupe ${a}`, team: null };
}
function computeBestThirdTeams(e) {
  const a = [];
  Object.entries(e).forEach(([e, t]) => {
    t[2] && a.push({ ...t[2], group: e.replace(/^Groupe\s+/i, ''), place: 3 });
  });
  const t = a
    .sort((e, a) => {
      if (a.points !== e.points) return a.points - e.points;
      const t = e.gf - e.ga,
        r = a.gf - a.ga;
      return r !== t ? r - t : a.gf !== e.gf ? a.gf - e.gf : e.team.localeCompare(a.team, 'fr');
    })
    .slice(0, 8);
  return new Set(t.map(e => e.team));
}
function getBestThirdSlot(e, a) {
  const t = [];
  Object.entries(e).forEach(([e, a]) => {
    a[2] && t.push({ ...a[2], group: e.replace(/^Groupe\s+/i, ''), place: 3 });
  });
  const r = t
    .sort((e, a) => {
      if (a.points !== e.points) return a.points - e.points;
      const t = e.gf - e.ga,
        r = a.gf - a.ga;
      return r !== t ? r - t : a.gf !== e.gf ? a.gf - e.gf : e.team.localeCompare(a.team, 'fr');
    })
    .slice(0, 8)[a];
  return r
    ? { type: 'team', label: r.team, stats: r }
    : { type: 'placeholder', label: `3ème qualifié #${a + 1}`, team: null };
}
function compareTeamStats(e, a) {
  if (a.points !== e.points) return a.points - e.points;
  const t = e.gf - e.ga,
    r = a.gf - a.ga;
  return r !== t ? r - t : a.gf !== e.gf ? a.gf - e.gf : e.team.localeCompare(a.team, 'fr');
}
function simulateMatchWinner(e, a, t) {
  return 'placeholder' === e.type || 'placeholder' === a.type
    ? { type: 'placeholder', label: t, team: null }
    : compareTeamStats(
          e.stats || { points: 0, gf: 0, ga: 0, team: e.label },
          a.stats || { points: 0, gf: 0, ga: 0, team: a.label }
        ) <= 0
      ? { type: 'team', label: e.label, stats: e.stats, source: t }
      : { type: 'team', label: a.label, stats: a.stats, source: t };
}
function computeKnockoutBracket(e) {
  const a = computeGroupStandings(e);
  if (Object.keys(a).sort((e, a) => e.localeCompare(a, 'fr')).length < 12) return null;
  const t = [
      { home: getKnockoutSlot(a, 'A', 1), away: getBestThirdSlot(a, 0) },
      { home: getKnockoutSlot(a, 'B', 2), away: getKnockoutSlot(a, 'C', 1) },
      { home: getKnockoutSlot(a, 'D', 2), away: getKnockoutSlot(a, 'E', 1) },
      { home: getKnockoutSlot(a, 'F', 2), away: getKnockoutSlot(a, 'G', 1) },
      { home: getKnockoutSlot(a, 'H', 2), away: getKnockoutSlot(a, 'I', 1) },
      { home: getKnockoutSlot(a, 'J', 2), away: getKnockoutSlot(a, 'K', 1) },
      { home: getKnockoutSlot(a, 'L', 2), away: getBestThirdSlot(a, 1) },
      { home: getBestThirdSlot(a, 2), away: getBestThirdSlot(a, 3) },
      { home: getKnockoutSlot(a, 'B', 1), away: getBestThirdSlot(a, 4) },
      { home: getKnockoutSlot(a, 'A', 2), away: getKnockoutSlot(a, 'D', 1) },
      { home: getKnockoutSlot(a, 'C', 2), away: getKnockoutSlot(a, 'F', 1) },
      { home: getKnockoutSlot(a, 'E', 2), away: getKnockoutSlot(a, 'H', 1) },
      { home: getKnockoutSlot(a, 'G', 2), away: getKnockoutSlot(a, 'J', 1) },
      { home: getKnockoutSlot(a, 'I', 2), away: getKnockoutSlot(a, 'L', 1) },
      { home: getKnockoutSlot(a, 'K', 2), away: getBestThirdSlot(a, 5) },
      { home: getBestThirdSlot(a, 6), away: getBestThirdSlot(a, 7) }
    ],
    r = t.map((e, a) => ({
      home: e.home,
      away: e.away,
      winner: simulateMatchWinner(e.home, e.away, `Vainqueur 16ème ${a + 1}`)
    })),
    o = Array.from({ length: 8 }, (e, a) => ({ home: r[2 * a].winner, away: r[2 * a + 1].winner })).map((e, a) => ({
      home: e.home,
      away: e.away,
      winner: simulateMatchWinner(e.home, e.away, `Vainqueur 8ème ${a + 1}`)
    })),
    n = Array.from({ length: 4 }, (e, a) => ({ home: o[2 * a].winner, away: o[2 * a + 1].winner })).map((e, a) => ({
      home: e.home,
      away: e.away,
      winner: simulateMatchWinner(e.home, e.away, `Vainqueur quart ${a + 1}`)
    })),
    s = [
      { home: n[0].winner, away: n[1].winner },
      { home: n[2].winner, away: n[3].winner }
    ].map((e, a) => ({
      home: e.home,
      away: e.away,
      winner: simulateMatchWinner(e.home, e.away, `Vainqueur demi ${a + 1}`),
      loser:
        'placeholder' === e.home.type || 'placeholder' === e.away.type
          ? { type: 'placeholder', label: `Perdant demi ${a + 1}`, team: null }
          : compareTeamStats(e.home.stats || {}, e.away.stats || {}) <= 0
            ? { type: 'team', label: e.away.label, stats: e.away.stats }
            : { type: 'team', label: e.home.label, stats: e.home.stats }
    })),
    c = { home: s[0].winner, away: s[1].winner },
    i = { home: s[0].loser, away: s[1].loser };
  return {
    rounds: [
      { name: '16èmes de finale', matches: t },
      { name: '8èmes de finale', matches: o.map(e => ({ home: e.home, away: e.away })) },
      { name: 'Quarts de finale', matches: n.map(e => ({ home: e.home, away: e.away })) },
      { name: 'Demi-finales', matches: s.map(e => ({ home: e.home, away: e.away })) },
      { name: 'Finale', matches: [c] },
      { name: '3ème place', matches: [i] }
    ]
  };
}
function renderKnockoutBracket(e) {
  const a = document.getElementById('knockout-grid');
  if (!a) return;
  const t = computeKnockoutBracket(e);
  if (!t)
    return void (a.innerHTML =
      '<p class="knockout-empty">Le tableau de phase finale sera disponible une fois les groupes constitués.</p>');
  const r = e => {
    const a = 'placeholder' === e.type,
      t = a ? e.label : e.label || e.team,
      r = a ? 'xx' : flagCodeForTeam(t);
    return `<div class="knockout-team ${a ? 'placeholder' : 'known'}">\n          <span class="knockout-team-name">${'xx' !== r ? `<span class="flag-tiny" aria-hidden="true"><img src="https://flagcdn.com/${r}.svg" alt="" loading="lazy" crossorigin="anonymous"></span>` : '<span class="flag-placeholder" aria-hidden="true"></span>'} <span>${t}</span></span>\n          <span class="knockout-score">-</span>\n        </div>`;
  };
  a.innerHTML = `<div class="knockout-rounds">${t.rounds.map((e, a) => `<div class="knockout-round" data-round="${a}">\n          <div class="knockout-round-title">${e.name}</div>\n          ${e.matches.map(e => `\n            <article class="knockout-match" role="listitem">\n              ${r(e.home)}\n              ${r(e.away)}\n              <div style="text-align: center; font-size: 10px; color: var(--color-text-muted); margin-top: var(--space-1);">-</div>\n            </article>`).join('')}\n        </div>`).join('')}</div>`;
}
function renderStandings(e) {
  const a = document.getElementById('standings-grid');
  if (!a) return;
  const t = computeGroupStandings(e),
    r = computeBestThirdTeams(t),
    o = Object.keys(t).sort((e, a) => e.localeCompare(a, 'fr'));
  o.length
    ? (a.innerHTML = o
        .map(
          e =>
            `\n          <div class="group-section" role="group" aria-label="${e}">\n            <h4 style="font-family: var(--font-display); font-size: var(--text-sm); color: var(--color-primary); margin: var(--space-4) 0 var(--space-2); text-transform: uppercase; letter-spacing: 0.05em;">${e}</h4>\n            <div class="card" style="overflow-x: auto;">\n              <table class="data-table standings-table">\n                <thead>\n                  <tr>\n                    <th>#</th><th>Équipe</th><th>J</th><th>V</th><th>N</th><th>D</th><th>BP:BC</th><th>Diff.</th><th>Pts</th>\n                  </tr>\n                </thead>\n                <tbody>${t[
              e
            ]
              .map((e, a) => {
                const t = e.gf - e.ga,
                  o = t > 0 ? `+${t}` : t;
                let n = 'eliminated';
                return (
                  a < 2 ? (n = 'qualified') : 2 === a && r.has(e.team) && (n = 'best-third'),
                  `\n            <tr class="${n}">\n              <td class="rank">${a + 1}</td>\n              <td class="team-cell">${'xx' !== flagCodeForTeam(e.team) ? `<span class="flag-tiny" aria-hidden="true"><img src="https://flagcdn.com/${flagCodeForTeam(e.team)}.svg" alt="" loading="lazy" crossorigin="anonymous"></span>` : '<span class="flag-placeholder" aria-hidden="true"></span>'} <span class="team-name">${e.team}</span></td>\n              <td>${e.played}</td>\n              <td>${e.won}</td>\n              <td>${e.drawn}</td>\n              <td>${e.lost}</td>\n              <td>${e.gf}:${e.ga}</td>\n              <td>${o}</td>\n              <td class="points"><strong>${e.points}</strong></td>\n            </tr>`
                );
              })
              .join('')}</tbody>\n              </table>\n            </div>\n          </div>`
        )
        .join(''))
    : (a.innerHTML =
        '<p class="text-center" style="color: var(--color-text-muted); padding: var(--space-8) 0;">Aucun classement disponible pour le moment.</p>');
}
function renderAllScorers(e) {
  const a = document.getElementById('scorers-list');
  if (!a) return;
  const t = {};
  e.forEach(e => {
    const a = e.homeTeam?.name || '',
      r = e.awayTeam?.name || '';
    (getGoalScorersCached(e) || []).forEach(o => {
      if (!o || !o.scorer) return;
      const n = 'home' === o.side ? a : r,
        s = normalizeScorerName(o.scorer) + '|' + normalizeScorerName(n);
      if (!s || '|' === s) return;
      (t[s] || (t[s] = { name: o.scorer, count: 0, team: null, country: null, goals: [] }),
        isBetterScorerDisplay(o.scorer, t[s].name) && (t[s].name = o.scorer),
        (t[s].count += 1),
        n && ((t[s].team = n), (t[s].country = n)));
      const c = 'home' === o.side ? r : a;
      t[s].goals.push({ minute: String(o.minute ?? ''), opponent: c || 'Adversaire inconnu', matchId: e.id, ownGoal: !!o.ownGoal });
    });
  });
  const r = Object.values(t).sort((e, a) => a.count - e.count || e.name.localeCompare(a.name, 'fr'));
  r.length
    ? (a.innerHTML = r
        .map((e, a) => {
          const t = e.team || '',
            r = t ? flagCodeForTeam(t) : 'xx',
            o =
              'xx' !== r
                ? `<span class="flag-tiny" aria-hidden="true"><img src="https://flagcdn.com/${r}.svg" alt="" loading="lazy" crossorigin="anonymous"></span>`
                : '<span class="flag-placeholder" aria-hidden="true"></span>',
            n = t ? `<span class="scorer-flag" aria-hidden="true">${flagForTeam(t)}</span>` : '',
            s = {};
          e.goals.forEach(e => {
            (s[e.opponent] || (s[e.opponent] = []), s[e.opponent].push({ minute: String(e.minute), ownGoal: e.ownGoal }));
          });
          const c = Object.entries(s)
            .sort((e, a) => e[0].localeCompare(a[0], 'fr'))
            .map(([e, a]) => {
              const t = a
                  .sort((e, a) => String(e.minute).localeCompare(String(a.minute), void 0, { numeric: !0 }))
                  .map(e => `${e.minute}'${e.ownGoal ? ' (CSC)' : ''}`)
                  .join(', '),
                r = flagCodeForTeam(e);
              return `<div class="scorer-goal-line">\n              <span class="scorer-goal-opponent">${'xx' !== r ? `<span class="flag-tiny" aria-hidden="true"><img src="https://flagcdn.com/${r}.svg" alt="" loading="lazy" crossorigin="anonymous"></span>` : '<span class="flag-placeholder" aria-hidden="true"></span>'} ${e}</span>\n              <span class="scorer-goal-minute">${t}</span>\n            </div>`;
            })
            .join('');
          return `\n          <article class="scorer-card" role="listitem">\n            ${n}\n            <div class="scorer-rank">${a + 1}</div>\n            <div class="scorer-main">\n              <div class="scorer-header">\n                ${o}\n                <span class="scorer-name">${e.name}</span>\n              </div>\n              <div class="scorer-country">${t} · ${e.count} but${e.count > 1 ? 's' : ''}</div>\n              <div class="scorer-goals-list">${c}</div>\n            </div>\n          </article>`;
        })
        .join(''))
    : (a.innerHTML =
        '<p class="text-center" style="color: var(--color-text-muted); padding: var(--space-8) 0;">Aucun buteur enregistré pour le moment.</p>');
}
function flagCodeForTeam(e) {
  const a = teamFlagCode(e);
  if ('xx' !== a) return a;
  const t = (STATIC_FALLBACK_TEAMS[2026] || []).find(a => a.name === e || a.name === translateTeamName(e));
  return t ? t.code : 'xx';
}
function flagForTeam(e) {
  const a = flagCodeForTeam(e);
  return 'xx' === a
    ? '<span class="flag-placeholder" aria-hidden="true"></span>'
    : `<img src="https://flagcdn.com/${a}.svg" alt="" loading="lazy">`;
}
function renderMatches(e, a) {
  const t = document.getElementById('matches-grid');
  if (!t) return;
  if (!e || !e.length)
    return (
      (t.innerHTML =
        '<p class="text-center" style="color: var(--color-text-muted); padding: var(--space-8) 0;">Aucun match à afficher.</p>'),
      void cleanupMatchMaps([])
    );
  const r = document.querySelector('.tab[aria-selected="true"]')?.dataset.filter || 'all',
    o = e.filter(e => {
      const a = e.homeTeam?.name,
        t = e.awayTeam?.name;
      return a && 'TBD' !== a && t && 'TBD' !== t;
    }),
    n = o.filter(e => {
      const a = getMatchEffectiveStatusCached(e);
      return 'finished' === r ? 'finished' === a : 'live' === r ? 'live' === a : 'upcoming' !== r || 'upcoming' === a;
    }),
    s = o.some(e => 'live' === getMatchEffectiveStatusCached(e));
  n.sort((e, a) => {
    const t = new Date(e.utcDate),
      o = new Date(a.utcDate);
    return 'finished' === r ? o - t : t - o;
  });
  const c = document.getElementById('global-live-badge');
  if ((c && (s ? c.classList.remove('hidden') : c.classList.add('hidden')), !n.length)) {
    const e = {
      live: 'Aucun match en cours pour le moment.',
      upcoming: 'Aucun match à venir.',
      finished: 'Aucun match terminé.'
    };
    return (
      (t.innerHTML = `<p class="text-center" style="color: var(--color-text-muted); padding: var(--space-8) 0;">${e[r] || 'Aucun match à afficher.'}</p>`),
      void cleanupMatchMaps([])
    );
  }
  const i = t.querySelector(':scope > p.text-center');
  i && i.remove();
  const l = n.map(e => String(e.id)),
    u = new Map();
  (t.querySelectorAll(':scope > article.match-card').forEach(e => {
    const a = e.dataset.matchId;
    l.includes(a) ? u.set(a, e) : e.remove();
  }),
    n.forEach((e, a) => {
      const r = String(e.id);
      let o = u.get(r),
        n =
          e.score && e.score.fullTime && null !== e.score.fullTime.home && void 0 !== e.score.fullTime.home
            ? e.score.fullTime.home
            : e.score && e.score.halfTime && null !== e.score.halfTime.home && void 0 !== e.score.halfTime.home
              ? e.score.halfTime.home
              : '-',
        s =
          e.score && e.score.fullTime && null !== e.score.fullTime.away && void 0 !== e.score.fullTime.away
            ? e.score.fullTime.away
            : e.score && e.score.halfTime && null !== e.score.halfTime.away && void 0 !== e.score.halfTime.away
              ? e.score.halfTime.away
              : '-';
      (null == n && (n = '-'), null == s && (s = '-'));
      const c = getMatchEffectiveStatusCached(e),
        i = 'live' === c,
        l = 'finished' === c,
        m = i ? 'EN COURS' : getStatusLabel(e.status),
        d = formatPhase(e.stage),
        h = e.venue || 'Stade à confirmer',
        p = STADIUM_CITIES[h] || '',
        g = STADIUM_COUNTRIES[h] || '',
        { local: f, paris: S } = formatMatchDateTime(e.utcDate, h),
        T = e.minute || (e.time_elapsed && !isNaN(Number(e.time_elapsed)) ? Number(e.time_elapsed) : ''),
        C = i && T ? ` (${T}')` : '',
        A = i ? 'status-dot live' : 'status-dot',
        y = `${e.id}-home`,
        w = `${e.id}-away`,
        E = previousScores.get(y),
        v = previousScores.get(w),
        D = void 0 !== E && void 0 !== v && (E !== n || v !== s);
      (previousScores.set(y, n), previousScores.set(w, s));
      const N = (function (e) {
        if (!e.length) return '';
        const a = e.filter(e => 'home' === e.side),
          t = e.filter(e => 'away' === e.side);
        return `\n          <div class="match-scorers">\n            <div class="match-scorers-col home">\n              ${a.map(e => `\n                <div class="match-scorer ${e.ownGoal ? 'own-goal' : ''}">\n                  <span class="scorer-minute">${e.minute}'${e.ownGoal ? ' (CSC)' : ''}</span>\n                  <span class="scorer-name">${e.scorer}</span>\n                </div>\n              `).join('')}\n            </div>\n            <div class="match-scorers-col away">\n              ${t.map(e => `\n                <div class="match-scorer ${e.ownGoal ? 'own-goal' : ''}">\n                  <span class="scorer-name">${e.scorer}</span>\n                  <span class="scorer-minute">${e.minute}'${e.ownGoal ? ' (CSC)' : ''}</span>\n                </div>\n              `).join('')}\n            </div>\n          </div>`;
      })(getGoalScorersCached(e));
      let F = '';
      F = l ? 'finished' : i ? 'live' : 'upcoming';
      const G = '-' === n && '-' === s ? (i ? '0 – 0' : 'vs') : n + ' – ' + s;
      if (o) {
        const r = t.children[a];
        (r !== o && t.insertBefore(o, r || null),
          D && (o.classList.add('flash'), setTimeout(() => o.classList.remove('flash'), 1e3)));
        const c = o.querySelector('.match-phase');
        c && (c.textContent = `${d}${e.group ? ' · ' + e.group : ''}`);
        const l = o.querySelector('.badge-status');
        l &&
          ((l.className = `badge-status ${F}`),
          (l.innerHTML = `<span class="${A}" aria-hidden="true"></span> ${m}${C}`));
        const u = o.querySelector('.match-score');
        u && ((u.textContent = G), u.setAttribute('aria-label', `Score ${n} à ${s}`));
        const T = o.querySelector('.match-scorers'),
          y = o.querySelector('.match-meta');
        if (N) {
          if (T) T.outerHTML = N;
          else if (y) {
            const e = document.createElement('div');
            ((e.innerHTML = N), o.insertBefore(e.firstElementChild, y));
          }
        } else T && T.remove();
        y &&
          (y.innerHTML = `\n              <div class="match-meta-row">📅 ${f}</div>\n              <div class="match-meta-row">🕐 <span class="time-paris">${S} (date/heure de Paris)</span></div>\n              <div class="match-meta-row">📍 ${h}${p ? ' - ' + p : ''}${g ? ' - ' + g : ''}</div>\n              ${i && lastRefreshTime.has(e.id) ? `<span class="refresh-indicator">Maj ${new Date(lastRefreshTime.get(e.id)).toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit', second: '2-digit' })}</span>` : ''}\n            `);
      } else {
        ((o = document.createElement('article')),
          (o.className = 'match-card'),
          (o.dataset.status = e.status),
          (o.dataset.matchId = r),
          (o.innerHTML = `\n            <div class="match-header">\n              <span class="match-phase">${d}${e.group ? ' · ' + e.group : ''}</span>\n              <span class="badge-status ${F}"><span class="${A}" aria-hidden="true"></span> ${m}${C}</span>\n            </div>\n            <div class="match-teams">\n              <div class="match-team">\n                <span class="flag" aria-hidden="true">${flagForTeamCached(e.homeTeam?.name || 'TBD')}</span>\n                <span class="name">${e.homeTeam?.name || 'TBD'}</span>\n              </div>\n              <div class="match-score" aria-label="Score ${n} à ${s}">${G}</div>\n              <div class="match-team">\n                <span class="flag" aria-hidden="true">${flagForTeamCached(e.awayTeam?.name || 'TBD')}</span>\n                <span class="name">${e.awayTeam?.name || 'TBD'}</span>\n              </div>\n            </div>\n            ${N}\n            <div class="match-meta">\n              <div class="match-meta-row">📅 ${f}</div>\n              <div class="match-meta-row">🕐 <span class="time-paris">${S} (date/heure de Paris)</span></div>\n              <div class="match-meta-row">📍 ${h}${p ? ' - ' + p : ''}${g ? ' - ' + g : ''}</div>\n              ${i && lastRefreshTime.has(e.id) ? `<span class="refresh-indicator">Maj ${new Date(lastRefreshTime.get(e.id)).toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit', second: '2-digit' })}</span>` : ''}\n            </div>\n          `));
        const c = t.children[a];
        t.insertBefore(o, c || null);
      }
    }),
    cleanupMatchMaps(n));
}
function renderNextWorldCups() {
  const e = document.getElementById('prochaines-list');
  if (!e) return;
  const a = new Date(),
    t = WORLD_CUPS.filter(e => new Date(e.end) > a).slice(0, 3);
  e.innerHTML = t
    .map(e => {
      const t = new Date(e.start),
        r = a >= t && a <= new Date(e.end);
      return `\n          <div class="wc-card ${r ? 'wc-active' : ''}" data-target="${e.start}">\n            <div class="wc-year">${e.year}</div>\n            <div class="wc-hosts">${e.hostNames}</div>\n            <div class="wc-countdown" id="cd-${e.year}">\n              ${r ? '<span class="live-badge pulse"><span class="dot"></span> EN COURS</span>' : '<span class="countdown-timer">Calcul...</span>'}\n            </div>\n          </div>\n        `;
    })
    .join('');
}
function updateCountdowns() {
  const e = new Date();
  WORLD_CUPS.forEach(a => {
    const t = document.getElementById(`cd-${a.year}`);
    if (!t) return;
    const r = new Date(a.start),
      o = new Date(a.end);
    if (e >= r && e <= o) t.innerHTML = '<span class="live-badge pulse"><span class="dot"></span> EN COURS</span>';
    else if (e < r) {
      const a = r - e,
        o = Math.floor(a / 864e5),
        n = Math.floor((a % 864e5) / 36e5),
        s = Math.floor((a % 36e5) / 6e4),
        c = Math.floor((a % 6e4) / 1e3);
      t.innerHTML = `<span class="countdown-timer">\n            <span class="cd-unit">${o}<small>j</small></span>\n            <span class="cd-unit">${String(n).padStart(2, '0')}<small>h</small></span>\n            <span class="cd-unit">${String(s).padStart(2, '0')}<small>m</small></span>\n            <span class="cd-unit">${String(c).padStart(2, '0')}<small>s</small></span>\n          </span>`;
    } else t.innerHTML = '<span class="badge-done">✅ Terminé</span>';
  });
}
function getCssVar(e) {
  return getComputedStyle(document.documentElement).getPropertyValue(e).trim() || '#f2f7f2';
}
function syncChartDefaults() {
  if (!window.Chart) return;
  const e = 'light' === document.documentElement.getAttribute('data-theme');
  ((Chart.defaults.color = getCssVar('--color-text')),
    (Chart.defaults.borderColor = e ? 'rgba(0,0,0,0.08)' : 'rgba(255,255,255,0.10)'));
}
let chartJsPromise = null;
function loadChartJs() {
  return window.Chart
    ? Promise.resolve(window.Chart)
    : chartJsPromise ||
        ((chartJsPromise = new Promise((e, a) => {
          const t = document.createElement('script');
          ((t.src = 'https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js'),
            (t.async = !0),
            (t.onload = () => {
              (syncChartDefaults(),
                window.Chart && (window.Chart.defaults.font.family = "'Inter', sans-serif"),
                e(window.Chart));
            }),
            (t.onerror = () => a(new Error('Failed to load Chart.js'))),
            document.head.appendChild(t));
        })),
        chartJsPromise);
}
function scheduleIdle(e) {
  'function' == typeof requestIdleCallback ? requestIdleCallback(e, { timeout: 2e3 }) : setTimeout(e, 1);
}
let palmaresObserver = null;
function initPalmaresLazy() {
  if (palmaresObserver) return;
  const e = document.getElementById('palmares');
  if (!e) return;
  const a = () => {
    loadChartJs()
      .then(() => initPalmaresChart())
      .catch(() => {});
  };
  'IntersectionObserver' in window
    ? ((palmaresObserver = new IntersectionObserver(
        e => {
          e.forEach(e => {
            e.isIntersecting && (palmaresObserver.disconnect(), a());
          });
        },
        { rootMargin: '200px' }
      )),
      palmaresObserver.observe(e))
    : scheduleIdle(a);
}
function initCharts() {
  (syncChartDefaults(), renderGoalsTable(chartMatches), initPalmaresLazy());
}
const themeObserver = new MutationObserver(() => {
  initCharts();
});
function renderGoalsTable(e) {
  const table = document.getElementById('goals-table');
  if (!table) return;
  const goalsByTeam = {};
  let totalGoals = 0;
  (e || []).forEach(match => {
    if (!match.score || !match.score.fullTime) return;
    const home = match.homeTeam?.name,
      away = match.awayTeam?.name,
      homeGoals = match.score.fullTime.home,
      awayGoals = match.score.fullTime.away;
    (null == home || null == homeGoals || isNaN(homeGoals) || ((goalsByTeam[home] = (goalsByTeam[home] || 0) + homeGoals), (totalGoals += homeGoals)),
      null == away || null == awayGoals || isNaN(awayGoals) || ((goalsByTeam[away] = (goalsByTeam[away] || 0) + awayGoals), (totalGoals += awayGoals)));
  });
  const badge = document.getElementById('total-goals-value');
  badge && (badge.textContent = totalGoals);
  const columns = {},
    zeroGoalTeams = [];
  Object.entries(goalsByTeam)
    .sort((e, a) => e[0].localeCompare(a[0], 'fr'))
    .forEach(([team, goals]) => {
      goals > 0 ? ((columns[goals] = columns[goals] || []), columns[goals].push({ team, goals })) : zeroGoalTeams.push({ team, goals: 0 });
    });
  const counts = Object.keys(columns)
      .map(Number)
      .sort((e, a) => a - e),
    hasZeroColumn = zeroGoalTeams.length > 0;
  table.innerHTML = '';
  if (0 === counts.length && !hasZeroColumn) {
    table.innerHTML = '<div class="goals-table-empty">Aucune équipe n\'a encore joué.</div>';
    return;
  }
  const headerRow = document.createElement('div');
  headerRow.className = 'goals-table-row goals-table-header-row';
  counts.forEach(count => {
    const header = document.createElement('div');
    header.className = 'goals-table-header';
    const teamCount = (columns[count] || []).length;
    header.innerHTML = `${count} But${count > 1 ? 's' : ''}<span class="goals-header-badge" aria-label="${teamCount} équipe${teamCount > 1 ? 's' : ''}">${teamCount}</span>`;
    headerRow.appendChild(header);
  });
  if (hasZeroColumn) {
    const header = document.createElement('div');
    header.className = 'goals-table-header';
    const teamCount = zeroGoalTeams.length;
    header.innerHTML = `0 But<span class="goals-header-badge" aria-label="${teamCount} équipe${teamCount > 1 ? 's' : ''}">${teamCount}</span>`;
    headerRow.appendChild(header);
  }
  table.appendChild(headerRow);
  const colLengths = Object.values(columns).map(e => e.length);
  hasZeroColumn && colLengths.push(zeroGoalTeams.length);
  const maxRows = Math.max(...colLengths);
  for (let rowIdx = 0; rowIdx < maxRows; rowIdx++) {
    const row = document.createElement('div');
    row.className = 'goals-table-row';
    counts.forEach(count => {
      const cell = document.createElement('div');
      cell.className = 'goals-table-cell';
      const teamData = columns[count]?.[rowIdx];
      if (teamData) {
        const teamEl = document.createElement('div');
        teamEl.className = 'goals-table-team';
        const code = flagCodeForTeam(teamData.team);
        if ('xx' === code) {
          const flag = document.createElement('span');
          flag.className = 'flag-placeholder flag';
          flag.setAttribute('aria-hidden', 'true');
          const name = document.createElement('span');
          name.textContent = teamData.team;
          teamEl.appendChild(flag);
          teamEl.appendChild(name);
        } else {
          const img = document.createElement('img');
          img.className = 'flag';
          img.src = `https://flagcdn.com/${code}.svg`;
          img.alt = '';
          img.loading = 'lazy';
          img.onerror = function () {
            this.style.display = 'none';
          };
          const name = document.createElement('span');
          name.textContent = teamData.team;
          teamEl.appendChild(img);
          teamEl.appendChild(name);
        }
        cell.appendChild(teamEl);
      }
      row.appendChild(cell);
    });
    if (hasZeroColumn) {
      const cell = document.createElement('div');
      cell.className = 'goals-table-cell';
      const teamData = zeroGoalTeams[rowIdx];
      if (teamData) {
        const teamEl = document.createElement('div');
        teamEl.className = 'goals-table-team';
        const code = flagCodeForTeam(teamData.team);
        if ('xx' === code) {
          const flag = document.createElement('span');
          flag.className = 'flag-placeholder flag';
          flag.setAttribute('aria-hidden', 'true');
          const name = document.createElement('span');
          name.textContent = teamData.team;
          teamEl.appendChild(flag);
          teamEl.appendChild(name);
        } else {
          const img = document.createElement('img');
          img.className = 'flag';
          img.src = `https://flagcdn.com/${code}.svg`;
          img.alt = '';
          img.loading = 'lazy';
          img.onerror = function () {
            this.style.display = 'none';
          };
          const name = document.createElement('span');
          name.textContent = teamData.team;
          teamEl.appendChild(img);
          teamEl.appendChild(name);
        }
        cell.appendChild(teamEl);
      }
      row.appendChild(cell);
    }
    table.appendChild(row);
  }
}
function renderRankingsAndScorers() {
  (renderStandings(chartMatches),
    renderAllScorers(chartMatches),
    scheduleIdle(() => renderKnockoutBracket(chartMatches)));
}
function initPalmaresChart() {
  const e = document.getElementById('palmares-chart');
  if (!e || !window.Chart) return;
  const a = [
    'rgba(46,204,113,0.7)',
    'rgba(240,192,64,0.7)',
    'rgba(231,76,60,0.7)',
    'rgba(52,152,219,0.7)',
    'rgba(155,89,182,0.7)',
    'rgba(149,165,166,0.7)'
  ];
  (palmaresChartInstance && palmaresChartInstance.destroy(),
    (palmaresChartInstance = new Chart(e, {
      type: 'bar',
      data: {
        labels: ['CONMEBOL', 'UEFA', 'CAF', 'CONCACAF', 'AFC', 'OFC'],
        datasets: [{ label: 'Titres', data: [10, 12, 0, 0, 0, 0], backgroundColor: a, borderRadius: 6 }]
      },
      options: {
        responsive: !0,
        maintainAspectRatio: !1,
        plugins: { legend: { display: !1 } },
        scales: { x: { grid: { display: !1 } }, y: { ticks: { stepSize: 1 } } }
      }
    })));
  const t = document.getElementById('palmares-legend');
  t &&
    (t.innerHTML = [
      'CONMEBOL — Confederación Sudamericana de Fútbol',
      'UEFA — Union des associations européennes de football',
      'CAF — Confédération africaine de football',
      'CONCACAF — Confederation of North, Central America and Caribbean Association Football',
      'AFC — Asian Football Confederation',
      'OFC — Oceania Football Confederation'
    ]
      .map(
        (e, t) =>
          `<div class="legend-item"><span class="legend-dot" style="background:${a[t]}"></span><span>${e}</span></div>`
      )
      .join(''));
}
function isLocalhostOrigin() {
  try {
    return /^(localhost|127\.0\.0\.1)(:\d+)?$/.test(location.hostname);
  } catch (e) {
    return !1;
  }
}
function matchesResultsChanged(e, a) {
  if (!e || !a) return !0;
  if (e.length !== a.length) return !0;
  const t = e =>
    `${e.scorer || e.player?.name || ''}|${e.minute ?? e.time ?? ''}|${e.side || (e.team?.name ?? e.team)}`;
  for (let r = 0; r < a.length; r++) {
    const o = e[r],
      n = a[r];
    if (o.id !== n.id) return !0;
    if (o.status !== n.status) return !0;
    if (o.finished !== n.finished) return !0;
    if (o.time_elapsed !== n.time_elapsed) return !0;
    if ((o.score?.fullTime?.home ?? null) !== (n.score?.fullTime?.home ?? null)) return !0;
    if ((o.score?.fullTime?.away ?? null) !== (n.score?.fullTime?.away ?? null)) return !0;
    const s = o.goals || [],
      c = n.goals || [];
    if (s.length !== c.length) return !0;
    if (s.length > 0 && c.length > 0 && s.map(t).sort().join('#') !== c.map(t).sort().join('#')) return !0;
  }
  return !1;
}
async function fetchAndRenderMatches(e, { skipMatchRender: a = !1, preferFresh: t = !1 } = {}) {
  if (!e) return;
  resetComputeCache();
  const r = STATIC_FALLBACK_MATCHES[e.year] || STATIC_FALLBACK_MATCHES[2026] || [],
    o = chartMatches,
    n = Date.now(),
    s = (t, r, s) => {
      (t.forEach(e => lastRefreshTime.set(e.id, n)), (chartMatches = t));
      const c = 'fallback' === lastMatchesSource && 'fallback' !== s;
      ((lastMatchesSource = s || lastMatchesSource),
        a || renderMatches(t, e),
        (matchesResultsChanged(o, chartMatches) || c) &&
          requestAnimationFrame(() => {
            (initCharts(), renderRankingsAndScorers());
          }),
        updateLastRefresh(r, s));
    };
  if (2026 === e.year)
    return (
      s(r, !0, 'fallback'),
      void fetchWorldcup26Matches({ preferFresh: t })
        .then(e => {
          e && e.length && s(e, !1, 'wcup2026.org');
        })
        .catch(() => {})
    );
  if ((await fetchWorldcup26Scorers(), isLocalhostOrigin()))
    try {
      const e = await fetchWithTimeout(
        `https://api.football-data.org/v4/competitions/WC/matches?nocache=${Date.now()}`,
        { headers: { 'X-Auth-Token': API_KEY } },
        8e3
      );
      if (!e.ok) throw new Error(`HTTP ${e.status}`);
      const a = await e.json(),
        t = a.matches.filter(e => 'IN_PLAY' === e.status || 'PAUSED' === e.status || 'LIVE' === e.status);
      (t.length &&
        (console.log('[API]', t.length, 'match(s) en cours —', new Date().toLocaleTimeString('fr-FR')),
        t.forEach(e =>
          console.log(
            '  ',
            e.homeTeam.name,
            e.score?.fullTime?.home ?? '-',
            '–',
            e.score?.fullTime?.away ?? '-',
            e.awayTeam.name,
            '|',
            e.minute ? e.minute + "'" : ''
          )
        )),
        s(a.matches, !1, 'football-data'));
    } catch (e) {
      s(r, !0, 'fallback');
    }
  else s(r, !0, 'fallback');
}
function updateLastRefresh(e, a) {
  const t = document.getElementById('last-update');
  if (!t) return;
  const r = new Date().toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
  t.textContent = e ? `Données statiques · ${r}` : `Mis à jour à ${r}`;
}
function updateFooterTimes() {
  const e = document.getElementById('footer-times');
  if (!e) return;
  const a = new Date(),
    t = new Intl.DateTimeFormat('fr-FR', {
      timeZone: 'Europe/Paris',
      weekday: 'long',
      day: 'numeric',
      month: 'long',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit'
    }).format(a),
    r = new Intl.DateTimeFormat('fr-FR', {
      timeZone: 'UTC',
      weekday: 'long',
      day: 'numeric',
      month: 'long',
      year: 'numeric',
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit'
    }).format(a);
  e.textContent = `Heure actuelle Paris : ${t} · UTC : ${r}`;
}
function selectBestTab(e) {
  const a = document.querySelectorAll('.tab');
  if (!a.length) return;
  const t = { live: 0, upcoming: 0, finished: 0 };
  (e || []).forEach(e => {
    t[getMatchEffectiveStatusCached(e)]++;
  });
  let r = 'upcoming';
  (t.live > 0 ? (r = 'live') : t.upcoming > 0 && (r = 'upcoming'),
    a.forEach(e => {
      const a = e.dataset.filter === r;
      (e.setAttribute('aria-selected', a ? 'true' : 'false'), e.setAttribute('tabindex', a ? '0' : '-1'));
    }));
}
function initTabs() {
  const e = document.querySelectorAll('.tab');
  e.forEach(a => {
    a.addEventListener('click', () => {
      (e.forEach(e => {
        (e.setAttribute('aria-selected', 'false'), e.setAttribute('tabindex', '-1'));
      }),
        a.setAttribute('aria-selected', 'true'),
        a.setAttribute('tabindex', '0'),
        currentWC && chartMatches.length && renderMatches(chartMatches, currentWC));
    });
  });
}
themeObserver.observe(document.documentElement, { attributes: !0, attributeFilter: ['data-theme'] });
let manualThemeOverride = !1;
function getDayOfYear(e) {
  const a = new Date(e.getFullYear(), 0, 0);
  return Math.floor((e - a) / 864e5);
}
function getSunTimesParis(e = new Date()) {
  const a = getDayOfYear(e),
    t = 23.45 * Math.sin(((360 / 365.25) * (a + 284) * Math.PI) / 180),
    r = (48.8566 * Math.PI) / 180,
    o = (t * Math.PI) / 180;
  let n = -Math.tan(r) * Math.tan(o);
  n = Math.max(-1, Math.min(1, n));
  const s = (180 * Math.acos(n)) / Math.PI / 15,
    c = 12 + s;
  function i(e, a) {
    const t = Math.floor(e),
      r = Math.round(60 * (e - t)),
      o = new Date(Date.UTC(a.getFullYear(), a.getMonth(), a.getDate(), t, r)),
      n = new Date(o.toLocaleString('en-US', { timeZone: 'Europe/Paris' }));
    return n.getHours() + n.getMinutes() / 60;
  }
  return { sunrise: i(12 - s, e), sunset: i(c, e) };
}
function applyAutoTheme() {
  if (manualThemeOverride) return;
  const e = new Date(),
    a = e.toLocaleString('en-US', { timeZone: 'Europe/Paris', hour12: !1 }),
    [t, r] = a.split(', ')[1].split(':').map(Number),
    o = t + r / 60,
    { sunrise: n, sunset: s } = getSunTimesParis(e),
    c = o < n || o >= s ? 'dark' : 'light';
  document.documentElement.getAttribute('data-theme') !== c &&
    (document.documentElement.setAttribute('data-theme', c), updateThemeIcons());
}
function updateThemeIcons() {
  const e = document.getElementById('icon-moon'),
    a = document.getElementById('icon-sun');
  e &&
    a &&
    ('light' === document.documentElement.getAttribute('data-theme')
      ? (e.classList.add('hidden'), a.classList.remove('hidden'))
      : (e.classList.remove('hidden'), a.classList.add('hidden')));
}
function initTheme() {
  const e = document.getElementById('theme-toggle');
  e &&
    (applyAutoTheme(),
    setInterval(applyAutoTheme, 6e4),
    e.addEventListener('click', () => {
      manualThemeOverride = !0;
      const e = document.documentElement.getAttribute('data-theme');
      (document.documentElement.setAttribute('data-theme', 'light' === e ? 'dark' : 'light'), updateThemeIcons());
    }));
}
function initScrollReveal() {
  const e = document.querySelectorAll('.section'),
    a = new IntersectionObserver(
      e => {
        e.forEach(e => {
          e.isIntersecting && e.target.classList.add('visible');
        });
      },
      { threshold: 0.1 }
    );
  e.forEach(e => a.observe(e));
}
async function refreshMatchesSilently(e) {
  if (!e || 2026 !== e.year) return;
  const a = chartMatches,
    t = await fetchWorldcup26Matches({ preferFresh: !0 });
  t &&
    t.length &&
    matchesResultsChanged(a, t) &&
    (resetComputeCache(),
    (chartMatches = t),
    selectBestTab(chartMatches),
    renderMatches(chartMatches, e),
    initCharts(),
    renderRankingsAndScorers(),
    updateLastRefresh(!1, 'worldcup26.ir'));
}
async function initSystem() {
  ((currentWC = await autoDiscoverWorldCup()),
    renderTeams(currentWC.year),
    scheduleIdle(() => {
      (renderNextWorldCups(), updateCountdowns(), initPalmaresLazy());
    }),
    resetComputeCache(),
    selectBestTab(STATIC_FALLBACK_MATCHES[currentWC.year] || STATIC_FALLBACK_MATCHES[2026] || []),
    fetchAndRenderMatches(currentWC),
    2026 !== currentWC.year ||
      (wc26FirstFetchDone && 'cache' !== lastWc26FetchSource) ||
      setTimeout(() => refreshMatchesSilently(currentWC), 2e3),
    initTabs(),
    initTheme(),
    initScrollReveal(),
    updateFooterTimes());
  const e = chartMatches.some(e => 'live' === getMatchEffectiveStatusCached(e)),
    a = currentWC.isLive ? (2026 === currentWC.year ? (e ? 6e4 : 12e4) : 6e4) : currentWC.isSoon ? 3e5 : 36e5;
  (refreshInterval && clearInterval(refreshInterval),
    (refreshInterval = setInterval(async () => {
      isPageVisible &&
        ((currentWC = await autoDiscoverWorldCup()),
        await fetchAndRenderMatches(currentWC, { preferFresh: 2026 === currentWC.year }),
        updateCountdowns(),
        updateFooterTimes());
    }, a)),
    document.addEventListener('visibilitychange', () => {
      ((isPageVisible = !document.hidden),
        isPageVisible &&
          setTimeout(async () => {
            currentWC &&
              ((currentWC = await autoDiscoverWorldCup()),
              2026 === currentWC.year
                ? await refreshMatchesSilently(currentWC)
                : await fetchAndRenderMatches(currentWC),
              updateCountdowns(),
              updateFooterTimes());
          }, 0));
    }),
    setInterval(async () => {
      const e = await autoDiscoverWorldCup(),
        a = currentWC?.isLive,
        t = e?.isLive;
      a !== t && ((currentWC = e), clearInterval(refreshInterval), initSystem());
    }, 36e5),
    setInterval(updateCountdowns, 1e3),
    setInterval(updateFooterTimes, 1e3));
}
document.addEventListener('DOMContentLoaded', initSystem);
